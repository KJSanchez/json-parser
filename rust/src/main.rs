#[allow(unused)]
fn main() {
    let mut a = "a".to_string();
    let b = "foo".to_string();

    let foo = "111111110";
    let mut index = 0;
    let foo = "\"\"";
    println!("{}", foo);

    // for b in (&a[..a.len() - 1]).chars() {
    //     println!("{}", b);
    // }

    // for (i, c) in a.chars().skip(1).enumerate() {
    //     println!("{}: {}", i, c);
    // }

    // a += "foo";
    // println!("{}", a.len());

    // for (i, a) in a.chars().enumerate() {
    //     println!("{}: {}", i, a);
    // }
}
